# Python
__pycache__/
*.pyc
.pytest_cache/

# Database (cache lokal, bukan sumber data)
*.db
*.db-shm
*.db-wal

# Logs
*.log

# Chart debug lama yang nyasar ke root (chart sekarang di-cache di Redis
# lewat tempfile, bukan ditulis permanen ke sini lagi -- lihat _render_chart
# di web/app.py). Root-only supaya tidak ikut meng-ignore assets/*.png atau
# web/static/*.png yang memang bagian dari source.
/*.png

# Env lokal
.env

# User Uploads
web/static/forum_uploads/

# Dokumen skripsi pribadi (draft, contoh skripsi kating/teman, pedoman) --
# bahan penulisan, bukan bagian dari source code aplikasi.
/skripsi najwan/
